export const countdownMath = () => {
  const THREE_DAYS_IN_MS = 0 * 24 * 60 * 60 * 1000;
  const NOW_IN_MS = new Date().getTime();

  const dateTimeAfterThreeDays = 0;

  return dateTimeAfterThreeDays;
};
